<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>

</head>
<body>
	<h2></h2>
	<h2></h2>
	<h2></h2>
	<h3></h3>
	<h3></h3>
	<h3></h3>
</body>
<script type="text/javascript">
/*
# 배열의 정렬 기능 메서드..
1. 배열의 sort 익명함수를 통한 정렬 처리..
	배열명.sort(function(left, right){ return left-right}); :오름차순 처리
 */
	var point = [];
	for(var cnt=1;cnt<=10;cnt++){
		// 0~100 랜덤 데이터 저장..
		point.push(parseInt(Math.random()*101));
	}
 	var h2Arr = document.querySelectorAll("h2");
 	h2Arr[0].innerText = point;
 	h2Arr[1].innerText = point.sort(function(left, right){ return left-right});
 	h2Arr[2].innerText = point.sort(function(left, right){ return right-left});
 	// ex)  5명에게 조사한 1~5 선호도 표시를 h3로 오름차순, 내림차순으로 출력하세요
 	//     5조
 	var favs = [];
 	for(var cnt=1;cnt<=5;cnt++){
 		favs.push(parseInt(Math.random()*5)+1);
 	}
 	var h3Arr = document.querySelectorAll("h3")
 	h3Arr[0].innerText = favs;
 	h3Arr[1].innerText = favs.sort(function(left, right){ return left-right});
 	h3Arr[2].innerText = favs.sort(function(left, right){ return right-left});
 	
 	
</script>
</html>